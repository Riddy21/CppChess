# FIXME: Change this to a chess interface file and add it to the makefile
# FIXME: Move all the python library imports to a build lib dir
from rules import *

class Game(object):
    def __init__(self, turn)